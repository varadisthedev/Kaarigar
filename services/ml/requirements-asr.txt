# Optional — only needed for the /asr fallback endpoint. Heavy (pulls in
# PyTorch); install this on top of requirements.txt only if you're deploying
# the ASR fallback tier. See README.md for the resource implications.
-r requirements.txt
torch==2.5.1
transformers==4.47.1
librosa==0.10.2.post1
soundfile==0.12.1
